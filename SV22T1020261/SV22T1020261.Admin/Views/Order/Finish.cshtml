@model OrderViewInfo
@{
    Layout = null;
}
<div class="modal-header text-bg-success">
    <strong class="modal-title">
        Xác nhận đơn hàng thành công
    </strong>
    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
</div>

<div class="modal-body">
    <div class="alert alert-success d-flex align-items-center">
        <i class="bi bi-info-circle me-2"></i>
        Bạn có chắc chắn xác nhận đơn hàng
        <span class="fw-bold text-dark ms-1 me-1">#@Model.OrderID</span>
        của khách hàng
        <span class="fw-bold text-dark ms-1">@Model.CustomerName</span> đã được giao thành công?
    </div>
</div>

<div class="modal-footer">
    <button type="submit" class="btn btn-primary" onclick="finishOrder(@Model.OrderID)">
        <i class="bi bi-check"></i> Chấp nhận
    </button>
    <button type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal">
        Đóng
    </button>
</div>
