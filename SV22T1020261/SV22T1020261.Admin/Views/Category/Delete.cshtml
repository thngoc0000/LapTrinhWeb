@model SV22T1020261.Models.Catalog.Category
@{
    ViewBag.Title = "Xóa loại hàng";
}

@section PageToolBar {
    @if (ViewBag.CanDelete)
    {
        <button type="submit" form="formDelete" class="btn btn-danger">
            <i class="bi bi-trash me-1"></i>
            Xóa
        </button>
    }

    <a href="~/Category" class="btn btn-secondary ms-1">
        <i class="bi bi-arrow-left"></i>
        Quay lại
    </a>
}

<div class="row justify-content-center">
    <div class="col-12">
        <div class="card card-danger card-outline">
            <div class="card-header">
                <h5 class="card-title mb-0 text-danger">
                    <i class="bi bi-exclamation-triangle me-2"></i>
                    @(ViewBag.CanDelete ? "Xác nhận xóa loại hàng" : "Thông báo")
                </h5>
            </div>

            <div class="card-body">
                <form id="formDelete" method="post" action="~/Category/Delete/@Model.CategoryID">

                    @if (ViewBag.CanDelete)
                    {
                        <div class="alert alert-warning d-flex align-items-center">
                            <i class="bi bi-info-circle me-2"></i>
                            Bạn có chắc chắn muốn xóa loại hàng sau không?
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-danger d-flex align-items-center">
                            <i class="bi bi-x-circle me-2"></i>
                            Không thể xóa loại hàng này vì có liên quan đến dữ liệu khác.
                        </div>
                    }

                    <dl class="row mb-0">
                        <dt class="col-sm-4">Tên loại hàng</dt>
                        <dd class="col-sm-8">@Model.CategoryName</dd>

                        <dt class="col-sm-4">Mô tả</dt>
                        <dd class="col-sm-8">@Model.Description</dd>
                    </dl>

                </form>
            </div>
        </div>
    </div>
</div>