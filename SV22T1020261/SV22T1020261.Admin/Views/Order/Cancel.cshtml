@model OrderViewInfo
@{
    Layout = null;
}
<div class="modal-header text-bg-warning">
    <strong class="modal-title">
        Xác nhận hủy đơn hàng
    </strong>
    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
</div>

<div class="modal-body">
    <div class="alert alert-warning d-flex align-items-center">
        <i class="bi bi-info-circle me-2"></i>
        Bạn có thực sự muốn hủy đơn hàng 
        <span class="fw-bold text-dark ms-1 me-1">#@Model.OrderID</span>
        của khách hàng
        <span class="fw-bold text-dark ms-1 me-1">@Model.CustomerName</span> hay không?
    </div>
</div>

<div class="modal-footer">
    <button type="submit" class="btn btn-primary" onclick="cancelOrder(@Model.OrderID)">
        <i class="bi bi-check"></i> Chấp nhận
    </button>
    <button type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal">
        Đóng
    </button>
</div>
